## 1.项目概述(Project Overview)

本项目旨在开发一个基于微服务架构的在线视频学习平台。系统采用前后端分离模式。
核心业务难点在于：如何利用云存储（OSS）和微服务权限控制，实现“付费内容”的保护与分发。即：
未付费用户无法获取视频资源，付费用户只能获得有时效性的播放权限。

### 技术栈要求

- 服务端(Backend): Go (Gin, gRPC), GORM, JWT
- 前端 (Frontend): React, Ant Design, Axios
- 存储(Storage): MySQL,阿里云OSS
- 运维(DevOps):Docker,Docker Compose

## 2.环境准备(Environment Preparation)

在开始写代码之前，请确保完成以下环境的准备。

### 2.1阿里云OSS开通与配置

本项目需要使用对象存储服务来存放课程视频和封面。请按照以下步骤自行开通阿里云OSS：

1.注册/登录：访问阿里云官网，注册并登录账号。

2.开通OSS服务：在控制台搜索"OSS”（对象存储），点击开通服务（按量付费，通常有免费额度或费用

很低）。

3.创建Bucket(存储空间):

- 进入OSS控制台，点击"创建Bucket"。

- Bucket Name:起一个全局唯一的名称（例如geekedu-yourname）。

- 地域（Region）：选择距离你最近的地域（如华东1-杭州）。

- 读写权限（ACL）：必须选择“私有”（Private）。这是为了防止未授权用户直接访问视频链接。

4.获取AccessKey:

- 为了代码能访问 OSS，需要AccessKey ID和AccessKeySecret。
- 推荐使用RAM访问控制创建一个子用户，并授予AliyunosSFullAccess权限。
- 重要：请妥善保存Key和Secret，实际生产环境中，这些不能直接提交到公开的Git仓库中。但此
- 次为了方便改作业，需要将配置文件上传。

## 3.项目目录结构规范(DirectoryStructure)

为了便于作业批改和统一管理，必须严格遵守以下目录结构，注意是git根目录：

```
geekedu-project/
├── web-server/          # web 接口服务（Gin JWT-->uid）
├── logic-server/        # 业务逻辑服务（gRPC）
├── common/              # 公共组件（Tools, Config, JWT）
├── proto/               # .proto 定义文件
├── frontend/            # 前端代码
│   ├── src/
│   ├── package.json
│   └── Dockerfile
├── deploy/              # 部署与运维配置
│   ├── docker-compose.yaml  # 核心编排文件
│   ├── mysql/           # MySQL 配置与初始化
│   │   └── init.sql     # 数据库初始化 SQL 脚本
│   └── nginx/           # Nginx 配置
│       └── nginx.conf
└── README.md            # 项目说明文档
```

## 4.系统架构设计(Architecture）

为了简化开发，我们将后端拆分为两个核心服务。系统运行时共需启动4个容器：

1. 前端容器（frontend):运行Nginx，托管构建后的React静态资源。

2. Web服务容器(web-server):基于Gin框架。作为HTTP流量入口，负责请求解析、路由转发、鉴

   权（Auth），并调用逻辑服务。

3. 逻辑服务容器（logic-server）:基于gRPC。负责核心业务逻辑（用户管理、课程管理、订单、

   阿里OSS交互）及数据库读写。

4. 数据库容器(mysql):基础设施，提供数据存储。

## 5. 功能需求说明 (Functional Requirements)

### 5.1 用户角色

系统包含两种角色：普通学员 (Student) 和管理员 (Admin)。

------

### 5.2 模块详情

#### A. 认证模块 (Auth)

- **注册 / 登录**：支持账号密码登录。
- **JWT 签发**：登录成功后返回 Token。
- **全局鉴权**：除登录 / 注册 / 课程列表外，其余接口均需验证 Token。

------

#### B. 课程管理 (讲师端)

- **发布课程**：填写课程标题、价格、简介，上传封面图到 OSS。
- **上传课程**：在课程下添加视频内容。一个课程可能包含多个视频，且视频文件较大，需要支持分片上传。请使用阿里云 OSS 的分片上传功能，使用鉴权 URL 直连上传。

------

#### C. 课程展示与购买 (学员端)

- **课程列表**：公开展示所有课程（封面图需使用 OSS 签名链接）。
- **模拟购买**：点击 “购买” 按钮，通过 API 将订单状态写入数据库（无需对接真实支付，调用即成功）。

------

#### D. 内容消费与播放 (核心考核点)

- 播放鉴权：
  1. 学员请求播放某视频。
  2. 后端校验该学员是否已购买该课程。
  3. 若已购买，后端调用 OSS SDK 生成一个预签名 URL (Presigned URL)，有效期 3600 秒。
  4. 前端拿到长链接，通过播放器播放。
  5. 注意：直接访问 OSS 原始地址应提示 `Access Denied`。

## 6. 接口定义参考 (API Reference)

建议使用 Restful 风格设计 Web Server 接口

> **注意**：接口仅供参考

| 方法 |            路径            |     描述     |                备注                |
| :--: | :------------------------: | :----------: | :--------------------------------: |
| POST |    `/api/v1/auth/login`    |   用户登录   |              返回 JWT              |
| GET  |     `/api/v1/courses`      | 获取课程列表 |              公开接口              |
| POST |     `/api/v1/courses`      |   发布课程   | 需要讲师权限，Form-data (含封面图) |
| POST |      `/api/v1/orders`      |   购买课程   |    请求体示例：`{course_id: 1}`    |
| GET  | `/api/v1/player/:video_id` | 获取播放地址 |   核心逻辑：返回带签名的 OSS URL   |

## 7. 系统设计要求 (System Design Requirements)

本项目不提供具体的数据库表结构设计，请根据功能需求自行设计。

你需要提交一份 **系统设计文档**（Markdown 格式），文档必须包含以下内容：

1. **系统架构图**：清晰展示 Frontend, Web Server, Logic Server, MySQL, OSS 之间的交互关系与数据流向。
2. **接口设计文档**：定义前后端交互的 API 接口（URL, Method, Request/Response Body）。
3. 数据库设计：
   - 数据库建表脚本（SQL），包含合理的索引、主键和外键设计。

## 8. 开发实施步骤

------

### 第一阶段：后端服务与

1. **Proto 定义**：编写 `.proto` 文件，定义 Web Server 与 Logic Server 之间的 RPC 接口。
2. 配置管理 (Configuration)：
   - 强制要求：所有敏感配置（AccessKey, SecretKey, DB Password）和环境配置（Host, Port）必须支持从配置文件或环境变量读取。
   - 严禁在代码中硬编码 Key 和 Secret。
3. Logic Server：
   - 实现用户注册登录逻辑。
   - 集成阿里云 OSS SDK：实现文件上传和签名 URL 生成逻辑。
   - 连接 MySQL 数据库。
4. Web Server：
   - 使用 Gin 搭建 HTTP 服务。
   - 连接 Logic Server 的 gRPC 接口。
   - 实现路由和中间件（JWT 鉴权）。

------

## 第二阶段：前端开发 (React Frontend)

1. **脚手架**：使用 Vite 创建 React 项目，放在 `frontend` 目录。

2. **基础组件**：引入 Ant Design，完成登录页和课程列表页。

3. 播放器：使用 

   ```
   react-player
   ```

    或 

   ```
   <video>
   ```

    标签。

   - 难点：只有当后端返回 200 和签名 URL 时，才渲染播放器。

------

## 第三阶段：容器化与联调

1. **Dockerfile**：为 Web Server 和 Logic Server 编写 Dockerfile。

2. **Nginx**：为前端编写 Dockerfile（Build -> Copy to Nginx）。

3. Docker Compose（一键启动）：

   在 

   ```
   deploy/docker-compose.yaml
   ```

    中定义所有服务。参考如下：

   ```
   version: '3.8'
   services:
     mysql:
       image: mysql:8.0
       environment:
         MYSQL_ROOT_PASSWORD: root
         MYSQL_DATABASE: geekedu
       volumes:
         - ./mysql/init.sql:/docker-entrypoint-initdb.d/init.sql
       ports:
         - "3306:3306"
   
     logic-server:
       build: ../logic-server  # 根目录下直接查找
       environment:
         # 通过环境变量注入敏感配置，不要写死在代码里
         OSS_ACCESS_KEY: ${OSS_ACCESS_KEY}
         OSS_SECRET_KEY: ${OSS_SECRET_KEY}
         OSS_ENDPOINT: "oss-cn-hangzhou.aliyuncs.com"
         OSS_BUCKET: "my-bucket-name"
         DB_HOST: mysql
       depends_on:
         - mysql
   
     web-server:
       build: ../web-server    # 根目录下直接查找
       ports:
         - "8080:8080"
       depends_on:
         - logic-server
   
     frontend:
       build: ../frontend
       ports:
         - "80:80"
   ```

4. **启动**：在 `deploy` 目录下运行 `docker-compose up --build`，即可启动整个系统。

## 9. 主要考核点

- 功能完善，符合需求说明中的功能需求。
- 代码结构清晰，有合理的注释，可扩展性高，易于维护。
- 错误处理完善（不直接 `panic`，有统一的错误码返回）。
- 配置文件管理（不硬编码 AccessKey/SecretKey）。
- OSS 安全与配置：Bucket 私有，使用服务端签名 URL。
- 配置管理：OSS Key/Secret 等配置未硬编码，支持通过环境变量或 Config 文件配置。
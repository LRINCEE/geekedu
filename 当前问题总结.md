## 🔴 严重问题（必须修复）

### 1. 完全没有单元测试 / 集成测试

**现状**：全项目 `0` 个 `_test.go` 文件。

**为什么致命**：面试官看到项目里一个测试都没有，会直接认为这不是"工程级"项目。Go 生态测试文化非常强，这是基本素养。

**改进方向**：

- 至少为 `service` 层写单元测试，使用 `testify/mock` 或 `gomock` mock掉 DAO 层
- 写 1~2 个 Handler 层的集成测试（`httptest` 包）
- 示例：`user_service_test.go` 测试注册重复用户名返回正确错误

**go**

复制

```go
// logic-server/service/user_service_test.go
func TestRegister_DuplicateUsername(t *testing.T) { ... }
func TestLogin_WrongPassword(t *testing.T) { ... }
```

------

### 2. `bypassRedis := true` —— 写死注释掉的死代码

**现状**：`course_service.go` 第88行，Redis缓存代码被一个布尔变量**永久绕过**，但相关代码仍残留在文件里（`if !bypassRedis { ... }` 永远不执行）：

**go**
复制
```go
// bypassRedis := true
bypassRedis := true  // 这让下面 100 行 Redis/singleflight 代码永远是死代码
```

**为什么致命**：这是"项目最大的技术亮点"（Redis缓存 + singleflight 防击穿），却被写死绕过了，这会让面试官觉得功能没完成。即使 Redis 配置不稳定，也不该用这种方式。

**改进方向**：
- 删掉 `bypassRedis` 这个变量，让 Redis 缓存逻辑真正生效
- 如果 Redis 连接可能不可用，应该用 `if err != redis.Nil && err != nil` 做优雅降级，而不是用布尔值完全绕过
------

### 3. `gRPC` 通信无 TLS，生产不可用

**现状**：`grpc_client/client.go` 中使用 `insecure.NewCredentials()`，内网通信完全明文。

**go**
复制
```go
conn, err = grpc.NewClient(addr,
    grpc.WithTransportCredentials(insecure.NewCredentials()), // ⚠️ 完全明文
)
```

**改进方向**：

- 对于简历项目，这个可以保留但必须**在 README 中说明**："生产环境应配置 mTLS，此处为本地开发简化版"
- 加分项：用 `credentials/tls` 实现自签名证书的 TLS 连接

------

### 4. 封面图通过 gRPC 传输二进制数据 —— 严重反模式

**现状**：`course_handler.go` 中，封面图被读成 `[]byte` 后通过 gRPC 消息传输：

**go**

复制



```go
coverData, err = io.ReadAll(file)  // 读完整文件到内存
// 然后通过 gRPC message 传过去
resp, err := grpc_client.CourseClient.CreateCourse(ctx, &pb.CreateCourseRequest{
    CoverData: coverData,  // ❌ 大文件走 gRPC 消息是反模式
    ...
})
```

**问题**：

1. gRPC 默认消息大小限制 4MB，大封面图直接失败
2. 把文件二进制塞进 proto message 不是正确用法，正确做法是 web-server 直接上传 OSS 或 logic-server 提供预签名 URL

**改进方向（两选一）**：

- 方案A：Logic Server 提供一个"生成封面上传预签名 URL"的 RPC，Web Server 拿到 URL 后返回给前端，前端直传 OSS
- 方案B（最简单）：Web Server 直接用 OSS SDK 上传，Logic Server 只负责存 `cover_key` 到数据库

------

## 🟡 中等问题（影响评分，建议修复）

### 5. 缺少请求参数校验（Binding Validation）

**现状**：多处参数校验不完整。

**go**

复制



```go
// course_handler.go
title := c.PostForm("title")
if title == "" {  // 只检查了 title
    response.Error(c, errcode.ErrInvalidParams)
    return
}
price, _ := strconv.ParseFloat(priceStr, 64)  // ⚠️ 忽略了解析错误，负数价格也能通过
```

**改进**：

- 用 `binding` tag + `validate` 库做完整校验：`binding:"required,min=1,max=256"`
- 价格应校验 `price >= 0`
- 分片数 `part_count` 应有上限（防止恶意发起 10000 个分片）

------

### 6. 错误处理中的字符串匹配 —— 脆弱设计

**现状**：`handleGRPCError` 函数通过**字符串匹配** gRPC 状态消息来路由错误：

**go**

复制



```go
switch st.Message() {
case "username already exists":      // ← 字符串硬编码
case "invalid credentials":
case "order processed,do not submit.":  // 还带标点符号，极易出错
```

**问题**：gRPC 有标准的 `codes.Code` 枚举，应该按 code 路由，而不是按 message 文本匹配。

**改进方向**：

**go**

复制



```go
// 错误的方式（现在）
switch st.Message() { case "xxx": }

// 正确的方式
switch st.Code() {
case codes.AlreadyExists:
    // 根据 code 区分，再看 message 细化
case codes.NotFound:
}
```

或者使用 gRPC `status.Details` 传递结构化错误详情。

------

### 7. `gin.Default()` 在生产中不合适

**现状**：`router.go` 使用 `gin.Default()` 会附带 gin 自带的 `Logger` 和 `Recovery` 中间件，直接把请求日志打到 stdout，没有结构化日志。

**改进方向**：

- 用 `gin.New()` + 自定义 `zap` 或 `slog` 中间件，输出 JSON 格式结构化日志
- 这是工业级项目的标准做法，简历上能写"集成 zap 结构化日志"

------

### 8. Redis 连接失败直接 `log.Fatalf` 崩溃

**现状**：

**go**

复制



```go
_, err := rdb.Ping(context.Background()).Result()
if err != nil {
    log.Fatalf("Failed to connect to Redis: %v", err)  // ← 崩溃
}
```

Redis 应该是**可降级**的组件（缓存不可用时降级到直接查库），不应该因为 Redis 挂了就让整个服务崩溃。

**改进方向**：连接失败打 warning 日志，服务继续启动，在使用 Redis 的地方做 `if err != nil` 降级。

------

### 9. `docker-compose.yaml` 中密码明文硬编码

**现状**：

**yaml**

复制



```yaml
environment:
  DB_PASSWORD: root  # ← 直接写死
```

即使通过 `${OSS_ACCESS_KEY}` 注入了 OSS Key，数据库密码还是硬编码了 `root`。

**改进方向**：统一用 `.env` 文件管理所有密钥，`deploy/` 目录下已经有 `.env` 了，把 `DB_PASSWORD` 也改成 `${DB_PASSWORD}`。

------

### 10. 缺少 API 限流中间件

**现状**：注册、登录接口没有任何限流，可以被无限暴力攻击。

**改进方向**：

- 用 `golang.org/x/time/rate` 或 `github.com/didip/tollbooth` 实现 IP 级别限流
- 登录接口加频率限制（如每 IP 每分钟最多 10 次）
- 简历上能写"实现登录接口 IP 级别限流，防止暴力破解"

------

## 🟢 加分项缺失（做了能显著提升简历含金量）

### 11. 没有 Swagger / OpenAPI 文档

工程级项目必须有 API 文档。用 `swaggo/swag` 注解生成，一条命令搞定，README 里加一行 [`http://localhost:8080/swagger/index.html`](http://localhost:8080/swagger/index.html)。

### 12. 没有健康检查接口

**go**

复制



```go
// 加一个
r.GET("/health", func(c *gin.Context) {
    c.JSON(200, gin.H{"status": "ok"})
})
```

K8s / docker 都需要 health endpoint，这是现代微服务的标配，docker-compose 已经对 MySQL 做了 healthcheck，服务本身也应该有。

### 13. gRPC 没有超时/重试中间件

现在每个 handler 手动 `context.WithTimeout(5s)` ，更好的做法是在 gRPC Client 层设置统一的默认超时和重试策略：

**go**

复制



```go
// 使用 grpc.WithDefaultServiceConfig 配置重试策略
grpc.WithDefaultServiceConfig(`{"methodConfig":[{"retryPolicy":{"maxAttempts":3,...}}]}`)
```

### 14. `ListCourses` 封面 URL 的 N+1 问题

**现状**：每次调用 `ListCourses`，会对每一个有 cover_key 的课程都调用一次 `ossutil.GenerateSignedURL`。10 个课程就是 10 次 OSS SDK 调用（虽然是本地计算，但仍是顺序循环）。

**改进**：可以用 `sync/errgroup` 并发生成，或者（更优雅）将签名 URL 缓存起来（签名 URL 有效期 3600s，可以缓存 3000s）。

### 15. 用 `log` 标准库，没有日志分级

全项目用的是 `log.Printf / log.Fatalf`，没有 `DEBUG/INFO/WARN/ERROR` 分级。改用 `uber-go/zap` 是 Go 后端的标准选择，会让项目显得专业很多。